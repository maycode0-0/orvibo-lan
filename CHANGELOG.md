# Changelog

本文件记录 Orvibo Lan 作为独立项目的用户可见变化。

## [Unreleased]

## [0.3.3] - 2026-08-05

### Fixed

- 门锁事件只有开锁方式时，指纹或密码开锁判定为室外开门，其他已上报的开锁方式判定为室内开门。
- 增加不包含设备号、用户、密码或属性值的门锁事件字段结构调试日志，用于兼容更多固件的具体开门用户字段。

## [0.3.2] - 2026-08-05

### Fixed

- 门锁实体识别改为依据 `battery_power` 和 `door_status` 能力组合，不再依赖设备记录来源标记，确保未知 `deviceType` 的 WiFi 门锁也注册最近开门方向和用户实体。

## [0.3.1] - 2026-08-05

### Fixed

- 修复仅通过属性状态发现、没有已知 `deviceType` 的 WiFi 门锁未注册“最近开门方向”和“最近开门用户”实体的问题。

## [0.3.0] - 2026-08-05

### Added

- WiFi 门锁新增“最近开门方向”和“最近开门用户”传感器，可区分室内/室外开门并显示推送中携带的用户名称或编号。

## [0.2.0] - 2026-08-04

### Added

- 属性型 WiFi 门锁从 `readtable.data.account` 获取临时 10002 凭据，建立双向 TLS 长连接并实时更新 `cmd=42` 门状态。
- 长连接支持心跳、断线重连、服务器证书指纹校验和配置卸载时的完整清理。

### Security

- 门锁事件只接受已发现设备的 `deviceId`，`uid` 仅在唯一匹配时作为兼容回退；临时凭据和原始事件不会写入日志。

## [0.1.1] - 2026-08-04

### Fixed

- MixPad 状态推送中的设备 `uid` 不再被误判为网关 UID，门磁开门和关门状态可通过 `cmd=42` 实时更新 Home Assistant 实体。

## [0.1.0] - 2026-08-03

### Added

- MixPad 家庭所有者切换后，可在集成选项中配置独立的 LAN 登录账号和密码；云端账号与 TCP 8088 本地账号可以分开使用。

## [0.0.11] - 2026-08-03

### Changed

- MixPad 拒绝本地登录时记录经过类型校验的数字状态码；非数字状态统一显示为 `invalid`，且不记录响应正文、账号或认证信息。

## [0.0.10] - 2026-08-03

### Changed

- 网关握手、登录、UID 校验、传输和心跳失败现在记录脱敏原因码，便于区分实体不可用的具体阶段，同时不暴露账号、UID、IP 或底层异常内容。

## [0.0.9] - 2026-08-03

### Fixed

- ORVIBO OAuth POST 返回不兼容响应时自动回退到厂商旧版 GET 接口，恢复配置、重认证和启动登录。

### Security

- OAuth 仍优先使用 POST；仅在厂商响应明确不兼容时使用 GET，且回退日志不包含账号、密码摘要或请求 URL。

## [0.0.8] - 2026-08-03

### Fixed

- LAN 可控设备改用实际网关连接判断可用性，不再被陈旧的云端离线标志误判为不可用。

## [0.0.7] - 2026-08-03

### Security

- OAuth 登录默认改用 POST 表单，避免用户名和密码摘要出现在 URL 查询串中。
- 配置项标题不再包含账号，并统一脱敏日志中的 IP、UID 和设备 ID。
- 网关在 Hello 返回 UID 时会在发送登录凭据前校验身份，错误信息不再包含完整 UID。
- LAN 状态推送绑定到实际来源网关，拒绝跨网关设备、无效命令、协议元数据覆盖、派生状态覆盖和畸形数值。

### Changed

- 保留部分 MixPad 非标准状态命令兼容，但仅在请求未关联、设备已知、网关归属匹配且字段校验通过时合并状态。
- README 和安全策略增加 TCP 8088 最小网络访问规则及厂商协议残余风险说明。

## [0.0.6] - 2026-08-02

### Fixed

- 兼容字符串命令号和未关联设备状态包，避免部分 MixPad 固件的门状态推送被静默丢弃。
- 统一识别 `deviceId`、`deviceID` 和 `device_id`，并补充网关连接、推送路由、状态合并及 HA 通知的调试日志（设备 ID 仅保留末四位）。

## [0.0.5] - 2026-08-01

### Fixed

- 兼容登录响应 UID 为空但云端已确认网关地址的 MixPad 固件，同时继续拒绝未确认的 UDP 候选地址。

## [0.0.4] - 2026-07-31

### Fixed

- 使用 MixPad Hello 响应中的网关 UID 完成身份校验，兼容登录成功响应不重复返回 UID 的固件。

## [0.0.3] - 2026-07-31

### Fixed

- 配置选项页兼容 Home Assistant 的只读 ConfigEntry 属性，不再返回 500。
- 启动后的区域分配通过线程安全任务调度执行，并避免重复移除一次性监听器。
- 云端只读设备的可用性由协调器刷新结果决定，不再被设备的离线字段误判。

### Changed

- 云端设备和网关快照会移除失效记录并关闭对应连接。
- 控制请求使用进程级原子序列号，避免并发命令关联冲突。
- LAN 状态通知在防抖窗口结束后统一发布。
- 云 API 拒绝请求时会重新认证一次，再向 Home Assistant 报告失败。
- ConfigEntry 使用版本 3，唯一 ID 按账号和家庭隔离。
- 实体可用性按 LAN 网关和云端数据源分别判断。
- 提供 `orvibo_lan.refresh_devices` 服务手动刷新拓扑。
- Ruff、mypy 和 pytest coverage 覆盖整个集成，覆盖率门槛为 62%。

### Security

- 所有 HTTPS 请求启用默认 TLS 校验。
- 网络异常和调试日志不记录凭据、会话密钥或完整控制 payload。

## [0.0.2] - 2026-07-31

### Changed

- 自动发布会等待完整 Validate 与发布包契约检查全部通过。

### Fixed

- Wi-Fi 设备不再引用不存在的 MixPad 网关，避免 Home Assistant 设备注册警告。

## [0.0.1] - 2026-07-31

Orvibo Lan 独立项目的首个发布版本。

### Added

- Home Assistant UI 配置、家庭选择、设备选择和再认证流程。
- Orvibo 云端拓扑读取与 MixPad 局域网控制。
- 多网关管理、UDP 地址发现、单 Reader TCP 请求路由和状态推送。
- Light、Cover、Climate、Fan、Sensor 和 Binary Sensor 平台。
- 集中式设备 Profile、来源感知状态仓库和资源关闭路径。
- HACS ZIP 发布、自动化质量检查和项目维护文档。
